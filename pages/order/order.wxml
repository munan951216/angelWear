<!-- 我的资产页面头部开始 -->
<van-nav-bar title="订单列表" left-arrow border="{{false}}" bind:click-left="onClickLeft" />
<!-- 我的资产页面头部结束 -->
<view>
  <van-tabs active="a" border="false">
    <van-tab title="{{ item }}" name="a" wx:for="{{ orderStatusList }}">
      <van-empty description="暂无订单" />
    </van-tab>
  </van-tabs>
</view>